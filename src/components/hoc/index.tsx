// factory function

// Enhancers: Wrap a component with additional functionality/props.
// Injectors: Inject props into a component.

// strongly recommend using Hooks wherever possible over higher-order components.